# task_2

// git inti -> to initate git file

// git add <file name> -> to add file

// git add . -> to add all files

// git commit -m "to add message" -> to add commit message

// git log -> gives full logs

// git log --oneline -> gives logs details in oneline

// git config --global user.name "Your Name" -> to set user name

// git config --global user.email "youremail@example.com" -> to set email id

// git status -> to get status of branch or anything to commit or not

// git branch -> to get details of all branch

// git branch <branch_name> -> to create branch

// git checkout <branch_name> -> to switch branch

// // not creating new anything in a branch

// git merge <branch_name> -> to merge 

// git remote add origin -> to connect to github 

// git push -u origin master -> to push to github
